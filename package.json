{
  "name": "hack-movie",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm i && npm --prefix backend i && npm --prefix frontend i",
    "back:dev": "npm run --prefix backend dev",
    "back:prod": "npm run --prefix backend prod",
    "front:dev": "npm run --prefix frontend dev",
    "front:prod": "npm run --prefix frontend prod",
    "dev": "run-p back:dev front:dev",
    "prod": "run-s front:prod front:dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@types/node-cron": "^2.0.2",
    "node-cron": "^2.0.3"
  }
}
